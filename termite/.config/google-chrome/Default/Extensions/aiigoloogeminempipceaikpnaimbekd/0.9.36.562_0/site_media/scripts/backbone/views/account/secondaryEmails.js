var SecondaryEmailsView,SecondaryEmailView;SecondaryEmailsView=Backbone.View.extend({events:{},initialize:function(){_(this).bindAll('add');this._views=[];this.collection.each(this.add);this.render();},add:function(se){var ev=new SecondaryEmailView({tagName:'li',model:se});this._views.push(ev);},render:function(){var self=this;_(this._views).each(function(ev){$(self.el).append(ev.render().el);});}});SecondaryEmailView=Backbone.View.extend({events:{},initialize:function(){},render:function(){$(this.el).html(this.model.get('email'));return this;}});